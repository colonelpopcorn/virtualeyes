import { storiesOf } from '@storybook/html';

storiesOf('MyComponent', module).add('Basic story', () => `<my-component first="Stencil" middle="'Don't call me a framework'" last="JS"></my-component>`);
